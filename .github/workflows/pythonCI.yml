name: Python CI/CD Pipeline
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: sleep for 200 seconds
        run: sleep 200
      - name: pull 
        run: git pull
      - name: install packages
        run: make python_install
      - name: lint
        run: make python_lint
      - name: test
        run: make python_test
      - name: format
        run: make python_format
      - name: delete
        run: python main.py query "DELETE FROM Goose WHERE id=3;
      - name: generate_and_push
        run: make generate_and_push
      - name: deploy
        run: make python_deploy